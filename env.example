# Database Configuration
# PostgreSQL connection string (Neon, Supabase, Railway, etc.)
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Session Security
# Generate a strong random string for production (minimum 32 characters)
SESSION_SECRET=your-secure-random-session-secret-here-change-in-production

# Application Environment
NODE_ENV=development
PORT=5000

# Domain Configuration (update for production)
DOMAIN=localhost
WWW_DOMAIN=www.localhost

# Google Cloud Storage (Optional - for image uploads)
# GCS_BUCKET_NAME=your-bucket-name
# GCS_PROJECT_ID=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

# Email Configuration (Optional - for contact form)
# RESEND_API_KEY=your-resend-api-key
# CONTACT_EMAIL=contact@yourdomain.com
